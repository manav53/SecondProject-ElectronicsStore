@model IEnumerable<string>

@Html.ActionLink("Home", "Index", "Nav", null,
    new { @class = "btn btn-block btn-default btn-lg" }) //cahnged from product list to index nav

@foreach (var link in Model) {
    @Html.RouteLink(link, new {
        controller = "Product",
        action = "List",
        category = link,
        page = 1
    }, new {
        @class = "btn btn-block btn-default btn-lg"
            + (link == ViewBag.SelectedCategory ? " btn-primary" : "")
    })
}
